players=[]

def readFile():
    infile=open("score_sheet.csv","r")
    infile.readline()
    infile.readline()
    counter=0
    for line in infile:
        data=line.split(",")
        if data[0]!="":
            players.append(Player(data[3],data[4],data[5],data[6],data[17]))
            
